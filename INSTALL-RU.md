# УСТАНОВКА — ULT's Loading Screen (ult-ld)

## Вариант 1. Вручную (рекомендуется)

1. Закрой Foundry VTT полностью (и клиент, и если запущен — сервер/ноду).
2. Открой папку с данными Foundry:
   - **Windows:** `%localappdata%\FoundryVTT\Data\modules\`
   - **Linux:** `~/.local/share/FoundryVTT/Data/modules/`
   - **macOS:** `~/Library/Application Support/FoundryVTT/Data/modules/`
   - **Если хостишь на сервере/Forge** — там, где лежат остальные модули (`Data/modules/`).
3. Распакуй архив **ult-ld.zip** в эту папку.
4. Проверь, что путь получился такой:
   ```
   Data/modules/ult-ld/module.json
   ```
   Именно так, **без** лишней вложенной папки вида `ult-ld/ult-ld/module.json` — это самая частая ошибка.
5. Запусти Foundry. В настройках мира открой **Add-on Modules** и убедись, что модуль **ULT's Loading Screen** появился в списке.
6. Включи его, сохрани настройки и перезайди в мир.

## Вариант 2. Установка по ссылке (если выкладываешь на GitHub)

1. Залей папку `ult-ld` в репозиторий GitHub (можно прямо как корень репо).
2. Возьми прямую ссылку на `module.json`, например:
   `https://raw.githubusercontent.com/ТВОЙ_НИК/ult-ld/main/module.json`
3. В Foundry: **Add-on Modules → Install Module**, вставь ссылку в поле **Manifest URL** и нажми Install.

---

## Настройка

Все настройки — в **Configure Settings → Module Settings → ULT's Loading Screen**, отдельными группами:

| Группа | Что настраивается |
|---|---|
| Кому показывать | Тумблеры «Показывать Мастеру» / «Показывать игрокам» |
| Время | Мин./макс. длительность, финальная задержка, аварийный таймаут |
| Отображение | Проценты, текст этапа, подсказки, заголовок |
| Палитра | 9 цветов + непрозрачность фона (нативные пикеры Foundry) |
| Медиа | Фоновая картинка, источник иконки (FA / свой файл), сам глиф и файл иконки |
| Конфликты | Поведение при активном другом лоадере, дополнительные id |

**Подсказки по лору** редактируются отдельным пунктом там же — **«Подсказки по лору → Редактировать подсказки»**. По одной подсказке на строку.

**Упрощённый режим** переключается:
- в личных настройках клиента (не требует перезахода), либо
- кнопкой в панели управления токенами (видна только Мастеру).

> ⚠️ Настройки из групп «Время», «Отображение», «Палитра», «Медиа» и «Конфликты» применяются **после перезахода в мир** — Foundry сама покажет предупреждение с кнопкой перезагрузки. Это сделано специально: оверлей строится на самом раннем этапе старта, раньше, чем доступны World-настройки.

---

## Стартовая палитра (под интерфейс BG3 HUD)

| Параметр | Значение |
|---|---|
| Фон — цвет 1 | `#151a20` |
| Фон — цвет 2 | `#0b0e12` |
| Непрозрачность фона | `94` |
| Заливка полосы | `#5b87ab` |
| Дорожка полосы | `#1e252d` |
| Границы | `#3d5a75` |
| Основной текст | `#e4eaf0` |
| Акцентный текст | `#8fa8c2` |
| Иконка | `#a9c4dd` |
| Свечение | `#456379` |

Вводятся в пикеры в формате R / G / B:

| Параметр | R | G | B |
|---|---|---|---|
| Фон 1 | 21 | 26 | 32 |
| Фон 2 | 11 | 14 | 18 |
| Заливка полосы | 91 | 135 | 171 |
| Дорожка полосы | 30 | 37 | 45 |
| Границы | 61 | 90 | 117 |
| Основной текст | 228 | 234 | 240 |
| Акцентный текст | 143 | 168 | 194 |
| Иконка | 169 | 196 | 221 |
| Свечение | 69 | 99 | 121 |

---

## Что нужно проверить в первую очередь

1. **Оверлей появляется** при входе в мир, F5 и после реконнекта.
2. **Не появляется** при смене сцены — это ожидаемое поведение.
3. Полоса держится на 90–99%, пока мир не готов, и доходит до 100% только после полной загрузки.
4. Полоса никогда не закрывается раньше 4 секунд.
5. Клики по канвасу, хотбару и окнам заблокированы, пока оверлей на экране. Escape у Мастера работает.
6. Подсказки сменяются по таймеру.
7. Упрощённый режим убирает анимации и свечение.

## Если что-то пошло не так

- **Оверлей не появился** — открой консоль браузера (F12) и поищи строки с `ult-ld`. Скорее всего, модуль отключился из-за конфликта: проверь настройку «При конфликте модулей».
- **Оверлей завис** — подожди аварийный таймаут (по умолчанию 2 минуты), он закроется сам. Затем увеличь значение или отключи проверку конфликтов.
- **Полоса стоит на месте** — так и задумано на этапе 90–99%: это значит, что мир ещё реально грузится. Если стоит очень долго при быстром интернете — проверь консоль на ошибки других модулей.

## Лицензия

MIT. Делай что хочешь, авторство — 4isfate.
